SELECT MAX(SAL),JOB
FROM EMP
GROUP BY JOB
HAVING MAX(SAL)>2600;

SELECT SAL
FROM EMP
GROUP BY SAL
HAVING COUNT(*)>=2;

SELECT SAL
FROM EMP
GROUP BY SAL
HAVING COUNT(*)>1;

SELECT HIREDATE
FROM EMP
GROUP BY HIREDATE
HAVING COUNT(*)>=2;


SELECT AVG(SAL),DEPTNO
FROM EMP
GROUP BY DEPTNO
HAVING AVG(SAL)<3000;






























